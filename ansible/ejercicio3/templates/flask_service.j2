[Unit]
Description=Flask Application on Port {{ port }}
After=network.target

[Service]
User=www-data
WorkingDirectory=/opt/ejercicio3
ExecStart=/usr/local/bin/gunicorn --workers 3 --bind 0.0.0.0:{{ port }} app:app

[Install]
WantedBy=multi-user.target
